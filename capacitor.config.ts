import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_angular_voyaga',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
